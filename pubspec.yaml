name: flutter_getx_hive_template
description: template

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: null
  get_storage:
  get_cli: null
  logging: ^1.0.2
  flutter_native_splash: ^1.2.4
  hive: ^2.0.4
  hive_flutter: ^1.1.0

dev_dependencies:
  hive_generator:
  build_runner:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
